# Full local environment. On Google Colab use requirements-colab.txt instead
# (Colab already provides a CUDA build of torch that must not be overwritten).
#
# POT 0.9+ is required for method="sinkhorn_log" and factored_optimal_transport.
POT>=0.9.0
numpy>=1.23
scipy>=1.9
scikit-learn>=1.1
scikit-image>=0.19
matplotlib>=3.5
torch>=1.13
torchvision>=0.14
scanpy>=1.9
anndata>=0.8
